# Command Runner Role

Bounded execution role for registered commands/tools delegated by a workflow.

## Lifecycle

Ephemeral by default. It receives explicit authority, inputs, boundaries and expected evidence; executes only the delegated capability; returns evidence; then may be discarded.

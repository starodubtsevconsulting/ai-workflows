# Why Coder?

A software team still needs somebody whose job is simply to turn a bounded decision into working code.

That is deliberately narrower than being architect, project manager, researcher or reviewer. If Coder starts deciding all of those things itself, every implementation task quietly becomes another design session and the workflow loses its boundaries.

Coder receives work with enough context to implement it, makes the implementation decisions that naturally belong inside that scope, uses the technical capabilities it is explicitly allowed to use, and reports back when done or blocked.

The point is not to make Coder unintelligent. The point is to spend its intelligence on implementation instead of asking it to own every question surrounding the implementation.